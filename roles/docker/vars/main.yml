---
docker_uninstall:
  - docker
  - docker-engine
  - docker.io
  - containerd
  - runc

install_deps:
  - ca-certificates
  - lsb-release
  - gnupg

docker_install:
  - docker-ce
  - docker-ce-cli
  - containerd.io
  - docker-compose-plugin
